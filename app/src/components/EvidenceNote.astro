---
import PedagogicalBlock from './PedagogicalBlock.astro';
interface Props { title?: string; }
const { title } = Astro.props;
---
<PedagogicalBlock type="evidence" title={title}><slot /></PedagogicalBlock>
