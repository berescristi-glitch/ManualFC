---
import PedagogicalBlock from './PedagogicalBlock.astro';
interface Props { title?: string; }
const { title } = Astro.props;
---
<PedagogicalBlock type="transfer" title={title}><slot /></PedagogicalBlock>
