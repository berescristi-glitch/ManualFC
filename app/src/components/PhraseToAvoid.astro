---
import PedagogicalBlock from './PedagogicalBlock.astro';
interface Props { title?: string; }
const { title } = Astro.props;
---
<PedagogicalBlock type="avoid" title={title}><slot /></PedagogicalBlock>
