# TASK-3304 — Implementarea componentelor educaționale și repararea defectului de hoisting MDX

## Stare

`DONE`

## Ce s-a construit

6 componente Astro noi/extinse (`ChapterOrganizer`, `ExplanationToggle`, `ElaborationBlock`, `RevealPrompt`, `QuickRecall`, `PredictPrompt`, plus extinderea `SectionNavigator` cu scroll-spy), `app/src/lib/educational-page.ts` (logica celor două Web Components — `explanation-toggle`, `section-navigator`), `app/src/styles/educational-page.css`, `BaseLayout.astro` actualizat să le încarce global.

## Defect real descoperit și reparat

Componentele Astro folosite din fișiere `.mdx` din `content/` (în afara `srcDir`) NU beneficiază de script/style hoisting la build — markup-ul randa corect, dar blocurile `<script>`/`<style>` ale componentelor dispăreau silențios din output (0 apariții în bundle-urile `_astro/*.js`/`*.css`). Descoperit prin verificare directă a HTML-ului compilat, nu presupus. Reparat prin centralizarea logicii/stilurilor în `app/src/lib/` și `app/src/styles/` (fișiere clar în interiorul `srcDir`), încărcate o singură dată din `BaseLayout.astro` — funcționează ca Web Components indiferent de unde a fost randat markup-ul în DOM.

## Validare

`npm run check`: 0 erori. `npm run build`: 92 pagini. Verificat programatic (grep pe HTML compilat) că CSS-ul (`.reveal-prompt`, `.chapter-organizer` etc.) și JS-ul (`customElements.define`) apar acum în output pe paginile de capitol. Regresie verificată: `principii/[slug].astro` (consumator preexistent al `SectionNavigator`) continuă să funcționeze identic.
