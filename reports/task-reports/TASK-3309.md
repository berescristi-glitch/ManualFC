# TASK-3309 — Acceptanță finală LIVE_BROWSER: audit real cu Playwright/Chromium

## Stare

`DONE`

## Ce s-a construit

Audit real de browser (nu `curl`/`vercel curl`) pe deployment-ul Preview real, folosind Chrome autentic condus prin Chrome DevTools Protocol, conectat la o sesiune Chrome autentificată de utilizator prin fluxul SSO al Vercel (nicio parolă/token citit de asistent — utilizatorul s-a autentificat manual în fereastra deschisă). 28 combinații pagină×viewport (7 pagini, 4 viewport-uri: 1440/1280/768/390), teste interactive (toggle de comprimare, Quick Recall, tastatură, deep-link-uri, reduced-motion, progressive enhancement fără JS), și un scan real Axe (`axe-core`, nu presupus).

## Defect real găsit și reparat (buclă de reparare)

**H1 tăiat la 390px pe CH-0106** — descoperit vizual din captură de ecran ("anxietate competiționa" cu ultima parte a cuvântului tăiată la marginea ecranului), confirmat prin măsurătoare (`scrollWidth` 431px vs `clientWidth` 375px). Root cause: `max-width:18ch` pe H1, calculat la un `font-size` mare (`clamp()`), poate produce o lățime mai mare decât containerul real la viewport-uri mobile — defect preexistent (confirmat prin `git diff` — CSS-ul și textul titlului identice de la baseline-ul PHASE-31 `35d2a36`), dar reparat aici pentru că afectează una dintre cele 4 pagini reprezentative testate în acest gate. Reparat cu `max-width:min(18ch, 100%);overflow-wrap:break-word` (commit `aff2017`), redeploy Preview nou (`dpl_2uzqqEHRNsZq5inH6mft8CVWAt9W`), reaudit complet — `scrollWidth` = `clientWidth` = 375 confirmat, 0 regresie pe toate celelalte 27 combinații.

## Defect critic descoperit ÎN AFARA scopului direct: corupere reală preexistentă a working tree-ului

În timpul unei rulări de `npm run build` (necesară pentru verificarea reparației H1), build-ul a eșuat cu `data/media/media-registry.json: Failed to parse JSON`. Investigație directă: fișierul era **0 bytes** pe disc. Scanare suplimentară a găsit încă 3 fișiere urmărite de git, toate **0 bytes**: `public/icons/icon-192.png`, `public/icons/icon-512.png`, `tests/test_task2802_workspace.py` — toate patru erau deja marcate `M` (modificate) în starea `git status` de la **începutul acestei sesiuni întregi**, înainte de orice modificare făcută de mine. Nu e o corupere cauzată de acest task. Reparat prin scriere directă a bytes-ilor din blob-ul `git show HEAD:<fișier>` (aceeași metodă sigură documentată în DEC-0070 — niciodată `git checkout`), verificat byte-cu-byte identic cu HEAD pentru toate cele 4 fișiere.

## Constatări în afara scopului (documentate, nereparate aici)

- `.principle-hero{width:100vw;...}` pe `principii/[slug].astro` cauzează overflow orizontal la toate viewport-urile — confirmat preexistent (fișier neatins de acest task), tehnică `100vw`-full-bleed clasică vulnerabilă la scrollbar. Necesită propriul task de reparație.
- Axe raportează `color-contrast` drept `incomplete` (nu violare) pe logo-ul site-wide (`Cunoaștere. Pedagogie. Practică.` suprapus peste imagine) — element global, neatins de acest task, prezent pe fiecare pagină a site-ului.

## Validare

`npm run check`: 0 erori. `npm run build`: 92 pagini. `pytest`: 505/505. Axe real (nu `NOT_RUN`): 0 violări pe 5 pagini testate, de două ori (înainte și după reparație). CRITICAL=0, MAJOR=0 pe toate paginile din scopul acestui task.
