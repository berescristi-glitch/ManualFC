# Raport de finalizare — TASK-2403

**Status:** DONE

## Scop și context

Remediere a singurului defect blocant identificat de auditul independent `TASK-2402`: `app/src/components/HomepageHero.astro` linia 16, CTA-ul hero al homepage-ului („Am nevoie acum") ducea la ruta fixture stricată `/probleme/lipsa-unghi-de-pasa` în loc de `/gold-standard/rapid` (destinația corectă, folosită deja de restul site-ului pentru aceeași etichetă).

## Ce s-a făcut

Modificare de o linie: `href="/probleme/lipsa-unghi-de-pasa"` → `href="/gold-standard/rapid"`, în `app/src/components/HomepageHero.astro`. Nicio altă modificare — nici CSS, nici structură, nici alt text.

## Verificare

- `npm run build` (din rădăcină): PASS, 72 pagini.
- Inspecție directă `dist/web/index.html`: 0 ocurențe rămase ale `href="/probleme/lipsa-unghi-de-pasa"`; 4 ocurențe corecte ale `href="/gold-standard/rapid"` (antet, hero, secțiunea „mode-split", și alte referințe consecvente pe homepage).
- `npm run check`: PASS, 0 erori.
- `python -m unittest discover -s tests -p "test_*.py"`: PASS, 417/417.
- `python scripts/validate_content.py --strict`, `python scripts/validate_project.py`, `python scripts/generate_task_registry.py --check`: toate PASS.

## Notă despre comitere

`HomepageHero.astro` e un fișier nou, necomis (parte a redesign-ului vizual în lucru documentat deja de `TASK-2401`/`DEC-0048`, alături de `AppHeader.astro`, `AppFooter.astro`, `index.astro`, `incepe-aici.astro`). Comiterea lui ar introduce în istoricul git întreaga componentă (imagini hero, CSS complet), nu doar linia reparată — exact problema pe care politica de design freeze o previne. Remedierea rămâne, deci, funcțională în working tree și în orice build local, dar necomisă, consecvent cu tratamentul aplicat deja celorlalte fișiere din același grup.

## Livrabile

- `app/src/components/HomepageHero.astro` — modificat (working tree, necomis).
- `reports/task-reports/TASK-2403.md` — acest raport.

## Ce urmează

Cu `TASK-2401`–`TASK-2403` închise, ciclul audit→remediere pentru această rundă e complet. Nu se deschide o nouă rundă de audit independent pentru o modificare mecanică de o linie deja specificată explicit de auditorul precedent — verificarea prin build+grep pe HTML-ul generat (nu doar citire de cod) e suficientă pentru acest tip de defect. Reziduul documentat deja de `TASK-2401`/`TASK-2402` (divergența HEAD/working-tree, etichetarea `/design-system`, migrarea `/exercitii`/`/probleme`, categoriile de taxonomie aspirațională) rămâne neschimbat.
