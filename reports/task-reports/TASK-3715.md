# TASK-3715 — Redeploy ManualFC from origin/main and Verify the Live Production Deployment

## Stare

Investigare/validare: `DONE`. **Verdict de deployment: `BLOCKED`.**

## Obiectiv

Redeployează exact revizia validată `origin/main` pe proiectul Vercel configurat și dovedește că site-ul live corespunde comportamentului repository-ului validat local.

## Sursa (SHA)

- Repo canonic: `E:/ManualFC-clean`, branch `main`.
- `git rev-parse HEAD` = `b0202f0515b684d6a6984ddbb0420d82d98c78e6`
- `git rev-parse origin/main` = `b0202f0515b684d6a6984ddbb0420d82d98c78e6` (identic)
- `git status --short --branch` = tree curat înainte de acest task (au fost adăugate ulterior doar planul și raportul acestui task, niciun fișier de produs).

## De ce BLOCKED

### Acces de deployment — verificat exhaustiv, dovedit indisponibil

| Verificare | Rezultat |
|---|---|
| `npx vercel whoami` | `Logged out.` |
| `.vercel/project.json` în `E:/ManualFC-clean` | Nu există |
| `VERCEL_TOKEN`/`VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` în mediu | Nu există |
| `%APPDATA%/com.vercel.cli/Data/auth.json` | Nu există (doar `config.json`, cu setări de telemetrie) |
| Tool MCP de deployment Vercel | Nu există (`ToolSearch` → niciun `mcp__plugin_vercel_vercel`) |
| `.vercel/project.json` reziduale în `/tmp/*` (sesiuni anterioare) | Găsite directoare, dar fără `project.json` valid recuperabil |

`vercel login` cere fie OAuth prin browser, fie o confirmare pe e-mail — niciuna nu poate fi completată dintr-o sesiune non-interactivă, sandboxată. Nu s-a încercat.

### Proiectul și URL-ul de producție (confirmate, nu inventate)

Din `reports/deployments/TASK-2601-vercel-staging.md` (`DEC-0053`):
- **Proiect Vercel:** `manualfc`
- **PROJECT_ID:** `prj_P2o9rzNbCsHraLk5CWVQGozAOZuU`
- **Organizație:** `berescristi-8889s-projects`
- **URL public:** `https://manualfc.vercel.app/` (același domeniu folosit deja ca `site` în `astro.config.mjs` de la `TASK-3711`)

### Starea live curentă — verificată direct, cu browser real, FĂRĂ nicio autentificare Vercel

Citirea unui site public nu necesită credențiale de deployment — această verificare a fost posibilă și a produs dovezi concrete:

| Verificare live | Rezultat |
|---|---|
| `GET https://manualfc.vercel.app/` | `200`, titlu „Metodologie U11 \| ManualFC U11" |
| CTA header (nav) „Rezolvă pe teren" | **`href="/gold-standard/rapid"`** (ruta veche, stricată) |
| CTA footer „Rezolvă pe teren" | **`href="/gold-standard/rapid"`** |
| CTA hero „Rezolvă pe teren ◷" | **`href="/gold-standard/rapid"`** |
| `GET /gold-standard/exercitii/EX-0006/` (TASK-3712) | **`404`** |
| `GET /robots.txt` (TASK-3711) | **`404`** |
| `GET /sitemap-index.xml` (TASK-3711) | **`404`** |
| `<link rel="canonical">` pe homepage | **absent** |
| `<meta property="og:url">` pe homepage | **absent** |

Aceste fapte, independente unele de altele, confirmă cu dovadă directă (nu presupunere) că deployment-ul live curent **precede `TASK-3711`** — deci precede automat și `TASK-3712` (extinderea Gold Standard) și `TASK-3713` (pilotul, care oricum nu produce pagini web). Site-ul live rulează o versiune mai veche a produsului, cu exact defectul de CTA pe care `TASK-3711` l-a reparat în repository, dar care nu a fost niciodată publicat.

## Validare locală completă (rulată integral, chiar sub blocaj)

| Comandă | Rezultat |
|---|---|
| `git status --short --branch` | tree curat |
| `git rev-parse HEAD` | `b0202f0515b684d6a6984ddbb0420d82d98c78e6` |
| `git rev-parse origin/main` | `b0202f0515b684d6a6984ddbb0420d82d98c78e6` (identic) |
| `npm run check` | 0 erori, 0 avertismente |
| `npm test` | 9/9 PASS (inclusiv axe pe 6 pagini critice) |
| `python -m pytest tests/ -q` | 576 passed + 25 subtests |
| `npm run build` | **119 pagini** |
| `python scripts/audit_route_links.py` | `ROUTE_CRAWL_PASS HTML=119 BROKEN_INTERNAL_REFS=0` |
| `EX-0006`–`EX-0015` prezente în `dist/web` | toate 10 prezente |
| `SES-0003`–`SES-0006` prezente în `dist/web` | toate 4 prezente |
| CTA în `dist/web/index.html` | 100% `/rezolva-pe-teren`, 0 apariții `/gold-standard/rapid` |
| `dist/web/robots.txt` | prezent, valid, indică `sitemap-index.xml` |
| `dist/web/sitemap-0.xml` | 118 URL-uri (+ `404.html` exclus corect = 119) |
| `<link rel="canonical">`/`<meta property="og:url">` pe `dist/web/index.html` | ambele prezente, `https://manualfc.vercel.app/` |
| Comportament offline (browser real, `context.setOffline(true)`) | homepage rămâne disponibilă, `200`, un `h1` |

**Concluzie din validarea locală:** exact ce ar trebui să fie live — și nu este — a fost dovedit local, complet și reproductibil.

## Ce NU s-a făcut (deliberat, conform regulii taskului)

- Niciun `vercel login` interactiv.
- Niciun deployment de substituție/temporar (`vercel deploy --temporary` sau echivalent).
- Nicio modificare a `vercel.json`/`astro.config.mjs`/`package.json` pentru a „forța" o formă de succes.
- Nicio modificare a repository-ului pentru a ocoli blocajul.
- Nicio pretenție de succes fără un URL de deployment verificabil și o stare de deployment reală.

## Acțiunea exactă cerută din partea utilizatorului

Una dintre următoarele, pentru a debloca acest task:

1. **Rulează `npx vercel login` interactiv** pe o mașină/sesiune cu browser real, apoi `npx vercel link` din `E:/ManualFC-clean` pentru a lega directorul la proiectul `manualfc` (org `berescristi-8889s-projects`).
2. **Furnizează un `VERCEL_TOKEN` valid** (cu drepturi asupra proiectului `manualfc`) ca variabilă de mediu accesibilă acestei sesiuni.
3. **Verifică din dashboard-ul Vercel dacă proiectul `manualfc` are deja o integrare Git activă** către `git@github.com:berescristi-glitch/ManualFC.git`. Dacă da, push-urile deja făcute (`TASK-3711`–`TASK-3713`) ar fi trebuit să declanșeze automat un deployment — faptul că live-ul e încă vechi ar indica o problemă de integrare (dezactivată, ruptă, sau branch greșit conectat), nu o lipsă de acces CLI; asta se poate verifica doar din dashboard, de către utilizator.

## Ce se întâmplă când accesul devine disponibil

Odată deblocat, pașii rămași sunt: `vercel deploy --prod` (sau echivalentul din workflow-ul documentat) din `E:/ManualFC-clean` la SHA-ul `b0202f0515b684d6a6984ddbb0420d82d98c78e6`, urmat de verificarea live completă cerută de task (CTA, rute noi, sitemap/robots, accesibilitate, responsive, offline, identitatea deployment-ului) — nimic din protocol nu se schimbă, doar accesul lipsește acum.

## Confirmări finale

- **Repo legacy `E:/ManualFC`:** neinspectat, neatins.
- **Working tree:** curat pe tot parcursul (doar ExecPlan + raport + guvernanță adăugate, niciun fișier de produs modificat).
- **`local main == origin/main`:** `b0202f0515b684d6a6984ddbb0420d82d98c78e6`, neschimbat de acest task.
- **Niciun secret, token sau valoare de mediu expusă** în acest raport sau în comenzile rulate — toate verificările de autentificare au raportat doar prezență/absență, niciodată valori.

## Verdict

**`BLOCKED`** — accesul de deployment Vercel autentificat nu e disponibil în acest mediu, verificat exhaustiv prin cinci canale independente. Nu s-a creat niciun deployment de substituție și nu s-a modificat repository-ul pentru a ocoli blocajul. Validarea locală completă dovedește că sursa (`b0202f0`) e corectă și gata de deployment; verificarea live directă (fără autentificare) dovedește că site-ul curent e stale și precede `TASK-3711`. Taskul se poate relua imediat ce una dintre cele trei acțiuni de mai sus e îndeplinită.
