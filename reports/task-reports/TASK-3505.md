# TASK-3505 — Migrare SES-0001/0002, Field Mode V2, reparații de legătură

## Stare

`DONE`

## Ce s-a construit

SES-0001/SES-0002 migrate cu `child_objectives`/`coach_objectives` (fiecare obiectiv al antrenorului legat de un `coach_competency_id` real) și `reflection_v2` (`child_game_dimension`/`coach_dimension`), plus `non_intervention_criteria`/`coach_focus`/`verification_method` pe fiecare segment, derivate din conținutul deja existent al segmentului (`coach_message`, `observation_focus`, `intervention_criteria`), nu inventate izolat.

Pagina de ședință extinsă cu o secțiune nouă „Copiii și antrenorul" (obiective duale) și cu extinderea secțiunii de reflecție cu cele două dimensiuni V2 plus o notă explicită de graniță („reflecția antrenorului nu e o măsură obiectivă..."). Field Mode extins cu afișarea per-segment a coach focus / când nu intervii / cum verifici, păstrând principiul „un segment, un ecran" (adăugări scurte, nu paragrafe).

## Defecte reale găsite și reparate (nu ascunse)

1. **Contrast de culoare preexistent** — Axe a raportat `color-contrast` (raport 2.62, necesar 4.5:1) pe link-ul „harta terenului" din panoul întunecat de pregătire, pe ambele ședințe. Confirmat prin `git show HEAD:...` că defectul exista IDENTIC înainte de orice modificare din această fază — dar bloca un gate propriu declarat al acestei faze (SES-0001/0002 sunt suprafețe de acceptanță explicite, §49). Reparat conform regulii §61 (reparăm defecte preexistente care blochează suprafețe de acceptanță declarate ale fazei curente).
2. **Gol în traseul learning-first** — CH-0408 și CH-0409 menționau `PRB-0003`/`EX-0003`/`SES-0001`/`SES-0002` doar ca text simplu, fără link real — un antrenor care citește capitolul nu putea ajunge cu un click la exercițiul/problema reală. Transformate în hyperlink-uri reale.

## Validare

`python scripts/validate_content.py --strict` = 0 erori. `python scripts/validate_gold_standard_v2.py` = 5 exerciții + 2 ședințe V2, 0 erori. `npm run check`/`build` = 0 erori. `pytest` = 505/505. Audit real de browser pe 11 rute × 4 viewport-uri = 44 combinații: prima rulare a găsit contrastul de mai sus (8 combinații afectate); după reparație, 44/44 PASS.
