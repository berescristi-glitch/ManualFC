# TASK-2908 — Content Depth Standard, Existing Product Mapping, Schema Impact Analysis

## Stare

`DONE`

## Ce s-a construit

`docs/knowledge/CONTENT_DEPTH_STANDARD.md` (4 straturi de prezentare — Teren/Acum, Înțelege, Aprofundează, Dovezi — peste modelul existent pe 2 niveluri din `CONTENT_STRATEGY.md`), `docs/knowledge/EXISTING_PRODUCT_KNOWLEDGE_MAPPING.md` (mapare completă a celor 8 probleme, 25 principii, EX-0001–EX-0005, SES-0001–SES-0002, ASM-0001, Decision Engine, Field Mode, Reflecție, media, cercetare pe stările `ALREADY_COMPATIBLE`/`NEEDS_METADATA_EXTENSION`/`NEEDS_SCHEMA_CHANGE`/`NEEDS_REFACTOR`), `docs/knowledge/KNOWLEDGE_SCHEMA_IMPACT_ANALYSIS.md` (fiecare câmp nou propus, cu impact retroactiv explicit `NICIUN regres`).

## Rezultat cheie

Niciun obiect canonic existent nu cade în `NEEDS_REFACTOR` distructiv — cel mai amplu caz identificat (coloana „COACH OBJECTIVES" pentru Session Contract V2) e o adăugire structurală, nu o rescriere. Toate schimbările de schemă propuse sunt aditive și opționale, verificate explicit împotriva regulii JSON Schema (proprietate absentă ≠ eroare dacă nu e în `required`).

## Regula celor patru straturi

`CONTENT_DEPTH_STANDARD.md` impune explicit „un obiect canonic, mai multe adâncimi de prezentare" — interzice crearea a patru articole separate pentru același concept, exact cerința din specificație §32.

## Impact asupra produsului curent

`RUNTIME_CHANGED: NO`. Nicio schemă `schemas/*.json` nu a fost editată — doar propuneri documentate.

## Validare

`python scripts/validate_project.py`: PASS, 0 erori. `python scripts/generate_task_registry.py --check`: PASS, registru reproductibil. `git diff --check`: PASS.
