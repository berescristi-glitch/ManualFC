# TASK-2801 — Release, ledger și arhitectură

## Verdict

`DONE / PASS` după repair gate autorizat.

Deploymentul Wave-3 acceptat `dpl_ZZvvHG9GTSSycW41RVkmVJUtT9aK`, asociat runtime-ului `ae04ed53929ce5a4a06a374a479451c40737e10c`, a fost promovat fără rebuild. Production este `dpl_GwmVnMkeBy7aMnAAQzBNgEFHYRv6`, READY, alias `manualfc.vercel.app`.

Smoke Production: rutele principale sunt 200, fixture-urile 404 și `X-Robots-Tag: noindex, nofollow` este păstrat. Search 12/20 și Field Mode la 390px trec. Verificarea explicită ulterioară a paginii `/gold-standard/exercitii/EX-0001` a găsit însă `document.querySelectorAll('main main').length = 1`. Cerința Gate A „no nested main” eșuează; conform instrucțiunii „If Gate A fails: STOP”, Wave-4 nu poate continua.

Ledgerul și schița de arhitectură au fost create înainte de detectarea tardivă a defectului; au devenit active numai după închiderea repair gate-ului.

Repair-ul `bc2f676` a stabilit BaseLayout ca unic proprietar al landmarkului, a adăugat validatorul pentru toate documentele construite și a trecut Preview-ul `dpl_7cXCQ9wiGZSnGF62STn6neY4zL1L`. Același artifact a fost promovat fără rebuild în Production `dpl_3iCpqviZUz9XYqZVr4SBX5fFFZvz`. Gate A final: PASS. `MANUALFC_PREMIUM_WAVE3_FINAL_BASELINE = bc2f67691266ac63fb2091f02bffac59ad19e463`; pointerul istoric `ae04ed5` rămâne neschimbat.

## Limite

Real field transfer rămâne `FIELD_INPUT_REQUIRED`. Account/cloud sync și observability nu sunt implementate de acest task.

## Validări

- `python scripts/generate_task_registry.py --check`: PASS, 228 taskuri;
- `python scripts/validate_project.py`: rulează după marcarea taskului `BLOCKED`;
- smoke HTTP: PASS;
- smoke browser semantic inițial: FAIL, păstrat ca istoric;
- repair Preview și Production re-audit: PASS, EX-0001 `main=1`, `nested_main=0`.
