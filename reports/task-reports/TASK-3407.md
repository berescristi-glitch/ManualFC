# TASK-3407 — Deployment Preview, acceptanță finală PHASE-32 și baseline ANTRENORUL_V1

## Stare

`DONE`

## Ce s-a construit

`.vercel/project.json` avea un link stale către un scope neautorizat pentru sesiunea curentă (`vercel deploy` a eșuat cu `Not authorized`). Diagnosticat direct (`vercel whoami`, `vercel project ls`) -- proiectul real `manualfc` există sub scope-ul contului curent (`berescristi-8889s-projects`). Rezolvat prin `vercel link --project manualfc --scope berescristi-8889s-projects` -- releg la un proiect deja existent, verificat, nicio credențială brută citită.

Deploy nou: `dpl_7y81gtiGxFQhuWr9kzDH5RZSjBoY`, confirmat `target: null` (Preview autentic, niciodată Production), `READY`. Verificat byte-cu-byte că fix-ul H1 (`max-width:min(18ch,100%);overflow-wrap:break-word`) e prezent în CSS-ul servit de Vercel, nu doar presupus identic cu local.

QA real de browser pe deployment-ul live (Chrome real, CDP): 5 rute reprezentative × 2 viewport-uri = 10 combinații, toate PASS -- 0 overflow, 0 violări Axe, `main` unic. Singura eroare de consolă observată: fetch eșuat pe `manifest.webmanifest` la PRIMA navigare către noul URL, redirecționat prin `vercel.com/sso-api` -- artefact tranzitoriu deja documentat identic în DEC-0075/DEC-0076 (cursă unică la stabilirea cookie-ului SSO pentru un deployment nou), nereprodus la rutele următoare din aceeași sesiune.

Raport final de acceptanță scris la `reports/audits/MANUALFC_PHASE32_ANTRENORUL_V1_ACCEPTANCE.md` (commit `47f870b`).

## Baseline înghețat

`MANUALFC_ANTRENORUL_V1_BASELINE = 00cd0f563...` (commit-ul de runtime care conține fix-ul H1, ultimul commit de cod înainte de commit-urile doar-documentație) -- vezi DEC-0077 pentru hash-ul complet verificat prin `git rev-parse`. Baseline-urile anterioare (`aff2017`, `96acb8c`) rămân neschimbate, nesuprascrise.

## Validare

`vercel curl` pe 5 rute -- toate 200. Verificare CSS servit vs. local -- identic. QA real de browser -- 10/10 combinații PASS.
