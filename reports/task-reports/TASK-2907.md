# TASK-2907 — Evidence Classification System și Research Production Pipeline

## Stare

`DONE`

## Ce s-a construit

`docs/knowledge/EVIDENCE_CLASSIFICATION_SYSTEM.md` și `docs/knowledge/RESEARCH_PRODUCTION_PIPELINE.md`.

## Constatare centrală

Vocabularul de dovezi candidat din specificație (`VERIFIED_FACT`, `SYSTEMATIC_REVIEW_SUPPORTED`, `HYPOTHESIS`, `FIELD_INPUT_REQUIRED` etc.) a fost verificat termen cu termen împotriva `schemas/claim-registry.schema.json` deja existent — 10 din 11 categorii candidate se mapează exact pe combinații deja existente de `claim_type`/`confidence`/`research_state`, fără niciun enum nou. Singura adăugire reală identificată: `PRACTICE_HEURISTIC` există azi doar ca text de UI, nu ca proprietate de schemă — propus ca extensie opțională, nu implementat.

Pipeline-ul de cercetare (`RESEARCH_PRODUCTION_PIPELINE.md`) confirmă că `research-question.schema.json` implementează deja lanțul cerut (`PROPOSED→SEARCHING→SCREENING→SYNTHESIZED→AUDITED→CLOSED/BLOCKED`) și că dosarele reale din `research/dossiers/` (verificat direct pe `ch-0103-decision-error.md`) urmează deja contractul complet cerut de specificație.

## Impact asupra produsului curent

`RUNTIME_CHANGED: NO`. Nicio schemă existentă nu a fost modificată.

## Validare

`python scripts/validate_project.py`: PASS, 0 erori. `python scripts/generate_task_registry.py --check`: PASS, registru reproductibil. `git diff --check`: PASS.
