# TASK-3406 — QA completă: validatori, build, teste, audit de conținut, QA real în browser

## Stare

`DONE`

## Ce s-a construit

Validare completă după integrarea celor 9 capitole noi:
- `npm run check` -- 0 erori/avertismente/hint-uri (96 fișiere).
- `npm run build` -- 101 pagini, 0 erori.
- `python -m pytest` -- 505 passed, 25 subtests passed.
- `python scripts/validate_project.py` -- 0 erori (293 taskuri).
- `python scripts/generate_task_registry.py --check` -- reproductibil.

## Defect real găsit și reparat (nu ascuns)

QA-ul real de browser (Playwright peste Chrome real conectat prin CDP, local dist/web servit static) a găsit overflow orizontal 5-12px la 390px pe 3 din 15 rute testate (`ch-0307`, `ch-0411`, indexul VOLUME-03). Root cause investigat direct (nu presupus): fix-ul H1 din DEC-0075 fusese aplicat DOAR pe `volum/01/[chapter].astro`; `volum/02/03/04` și indexurile 03/04 păstraseră `max-width:18ch`/`20ch` fără `overflow-wrap:break-word`. Titlurile mai lungi din PHASE-32 au declanșat vizibil acest defect latent preexistent. Reparat identic în toate cele 5 fișiere afectate (commit `00cd0f5`, separat de commit-ul de conținut).

**Fals pozitiv investigat și infirmat, documentat pentru transparență:** o primă re-verificare post-fix a arătat overflow-ul încă prezent -- investigație suplimentară a identificat cache-ul browserului real (nu al serverului) ca sursă, nu un fix incomplet. Verificat cu bypass explicit de cache; overflow confirmat 0 după aceea.

**Rezultat final:** 15 rute × 4 viewport-uri (1440/1280/768/390) = 60 combinații, toate PASS -- 0 overflow, `main`/`h1` unic, Axe real 0 violări (WCAG 2.0/2.1 A/AA), 0 erori de consolă/pagină. Deployment Preview nou creat și reverificat live cu parități identice (vezi TASK-3407).

## Validare

Toate comenzile de mai sus rulate cu cod de ieșire 0; rezultatele complete de browser salvate în `phase32_qa3_results.json` (scratchpad, verificat programatic -- 0 din 60 înregistrări cu vreo problemă).
