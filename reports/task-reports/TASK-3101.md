# TASK-3101 — PHASE-31 preflight și inspecția arhitecturii de conținut

## Stare

`DONE`

## Ce s-a construit

Preflight de integritate a cercetării (regulă obligatorie §2 din specificație): `research/sources.json`/`claims.json`/`citations.json` verificate byte-identic cu blob-urile Git HEAD, JSON valid, `validate_content.py`/`validate_project.py` 0 erori — niciun risc de corupere reziduală din incidentul documentat în DEC-0070.

## Constatare centrală care a schimbat scopul fazei

Inspecția arhitecturii reale de conținut (`content/volume-01/*.mdx`, `data/content/volumes/volume-01.json`) a găsit că **5 din cele 12 capitole planificate în blueprint există deja**, publicate și mature: CH-0101 (copilul), CH-0102 (percepție/atenție), CH-0103 (eroare/decizie), CH-0104 (motivație/mesaj pedagogic), CH-0105 (grup/diferențiere/transfer). Conform regulii §5 („Do not redesign these systems unless a genuine conflict is found”), scopul de autorizare a fost adaptat onest: PHASE-31 nu redactează 12 capitole noi, ci **5 capitole cu adevărat noi** (emoții, relație, comunicare generală, părinți, metacogniția copilului) care extind volumul existent, plus verificare de compatibilitate (nu rescriere) a celor 5 deja publicate.

## Impact asupra produsului curent

`RUNTIME_CHANGED: NO` (doar la acest task — inspecție, nu modificare).

## Validare

`python scripts/validate_project.py`: PASS, 0 erori. `python scripts/generate_task_registry.py --check`: PASS, registru reproductibil. `git diff --check`: PASS.
