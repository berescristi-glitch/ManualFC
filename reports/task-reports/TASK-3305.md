# TASK-3305 — Pilot pe 3 capitole reprezentative și reparare defect de fidelitate

## Stare

`DONE`

## Ce s-a construit

Aplicarea sistemului de componente pe 3 capitole cu caracteristici diferite: CH-0101 (conceptual, fără elaborare separată → fără `ExplanationToggle`, decizie corectă nu forțată), CH-0103 (practic/procedural, cu `PredictPrompt` + `ExplanationToggle`), CH-0106 (relațional/emoțional, deja folosit ca exemplu de referință în inspecția inițială).

## Defect real descoperit și reparat

Prima editare a `chapter-03.mdx` a eliminat accidental paragraful de deschidere al scenariului („Un copil primește între linii...") — pierdut la restructurarea zonei de deschidere a fișierului. Descoperit prin verificare programatică linie-cu-linie contra `HEAD` (nu presupus corect), reparat imediat prin reintroducerea paragrafului în poziția corectă (înaintea `PredictPrompt`, care îl referă explicit), reverificat.

## Validare

Verificare linie-cu-linie automată: fiecare linie non-titlu din versiunile originale (`git show HEAD:...`) există verbatim în fișierele editate — 0 linii lipsă pe toate cele 3 capitole pilot, după reparare. `npm run check`/`build`: PASS.
