# TASK-3103 — Expunerea în produs a celor 5 capitole noi

## Stare

`DONE`

## Ce s-a construit

Actualizare aditivă, minimă, în tiparul deja existent, a trei fișiere runtime: `app/src/pages/volum/01/[chapter].astro` (5 importuri MDX noi + 5 intrări în harta de capitole + extinderea `getStaticPaths`), `app/src/pages/volum/01/index.astro` (5 rânduri noi în tabla de materii, eyebrow actualizat la „10 CAPITOLE”), `content/volume-01/manifest.json` (5 intrări noi).

## Verificare reală

`npm run check` (astro check): 0 erori, 0 avertismente, 89 fișiere. `npm run build`: 92 pagini generate cu succes, inclusiv toate cele 10 rute `/volum/01/ch-01xx/`. Verificare HTTP directă pe serverul de preview: toate cele 5 rute noi răspund 200, titlu corect randat, un singur `<main>` per document, ierarhie de titluri (h1→h2→h3) fără salturi, toate cele 10 legături prezente pe pagina index.

## Limitare onestă

Nu a fost posibil un audit vizual/accesibilitate complet prin browser real (Playwright) — serverul MCP relevant s-a deconectat mai devreme în această sesiune și nu a fost disponibil. Verificarea s-a bazat pe inspecție HTTP/HTML directă, nu pe capturi vizuale la 1440/1280/768/390. `AXE = NOT_RUN`, raportat onest, nu presupus PASS.

## Impact asupra produsului curent

`RUNTIME_CHANGED: YES` — primul task din PHASE-30/31 cu schimbare reală de runtime, exact cum permite explicit specificația PHASE-31 (spre deosebire de PHASE-30, care a rămas strict research).

## Validare

`npm run check`: PASS, 0 erori. `npm run build`: PASS, 92 pagini. `python scripts/validate_project.py`: PASS, 0 erori. `git diff --check`: PASS.
