# TASK-3029 — Matrice R3 finale și integrare registru

## Stare

`DONE`

## Ce s-a construit

`PEDAGOG_RESEARCH_COVERAGE_MATRIX_R3.md`, `COACH_RESEARCH_COVERAGE_MATRIX_R3.md`, `COMPETENCY_EVIDENCE_MATRIX_R3.md` (versiunile anterioare păstrate ca istoric). Integrarea a 78 de claim-uri noi și 76 surse noi în registrele reale, cu deduplicare (3 surse fuzionate cu ID-uri canonice existente).

## Incident tehnic și rezolvare

În timpul integrării s-a descoperit corupere reală pe disc a `research/claims.json`/`citations.json`, cauzată de un defect determinist al filtrului de conversie linie-terminare al Git pentru Windows (`core.autocrlf=true` + `.gitattributes eol=lf`), reprodus de două ori la `git checkout`. Rezolvat prin scriere directă a octeților curați din blob-ul Git, ocolind checkout-ul — fără nicio modificare de configurare git. Documentat integral în raportul de acceptanță Wave-3 §2, ca risc real de mediu pentru viitor, nu doar remediat tăcut.

## Impact asupra produsului curent

`RUNTIME_CHANGED: NO`.

## Validare

`python scripts/validate_project.py`: PASS, 0 erori. `python -c "jsonschema.validate(...)"` pe toate cele trei registre: PASS, 361 surse/380 claim-uri/408 citări. `python scripts/generate_task_registry.py --check`: PASS, registru reproductibil. `git diff --check`: PASS.
