Lucrează din rădăcina acestui repository.

Citește integral, în această ordine:

1. `AGENTS.md`;
2. `CODEX.md`;
3. `MASTER_EXECUTION_PROMPT.md`;
4. `PLANS.md`;
5. toate fișierele din `docs/`;
6. schemele din `schemas/`;
7. `TASK_REGISTRY.json`.

Execută taskul de inițializare `TASK-0001` și creează primul ExecPlan. Inspectează mediul și repository-ul, propune arhitectura tehnică, extinde registrul de la faze la taskuri verificabile și creează structura persistentă necesară proiectului.

Cerințe pentru această primă execuție:

- nu scrie încă volumele;
- nu genera conținut demonstrativ doar pentru a umple directoarele;
- stabilește întâi sistemele canonice de date, cercetare, citare, vizual, testare și build;
- păstrează grupele 2015 și 2016 ca o singură categorie de aproximativ 10–11 ani;
- definește clar cum va fi fundamentată fiecare indicație transmisă copilului;
- creează taskuri suficient de mici pentru a putea fi validate și revizuite;
- pentru conținut, ținta normală este un capitol, 3–5 exerciții sau 1–2 ședințe per task;
- rulează validările disponibile;
- actualizează `TASK_REGISTRY.json`, `TASK_HISTORY.jsonl`, `PROJECT_STATUS.md`, `DECISIONS.md` și raportul taskului;
- nu marca taskul `DONE` dacă outputurile nu există sau verificările nu au fost rulate.

La finalul acestei execuții, raportează concis:

- fișierele create sau modificate;
- arhitectura aleasă și motivul;
- numărul taskurilor generate;
- validările rulate;
- următorul task `READY`;
- progresul global ponderat.
